import React from "react";
import Chip from "@material-ui/core/Chip";

const Chipy = ({ label }) => {
  return (
    <div>
      <h1>HOla, {label}</h1>
      <div>
        <input type="text" />
      </div>
    </div>
  );
};

export default Chip;
